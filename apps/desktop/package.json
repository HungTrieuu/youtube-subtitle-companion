{
  "name": "@youtube-subtitle-companion/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "node ./scripts/dev.mjs",
    "build": "node ./scripts/build.mjs",
    "package": "pnpm run build && electron-builder --config electron-builder.yml",
    "package:linux": "pnpm run build && electron-builder --config electron-builder.yml --linux AppImage",
    "package:win": "pnpm run build && electron-builder --config electron-builder.yml --win nsis"
  },
  "dependencies": {
    "@youtube-subtitle-companion/shared": "workspace:*",
    "electron-store": "^10.0.1",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "electron": "^34.0.0",
    "electron-builder": "^25.1.8"
  }
}
